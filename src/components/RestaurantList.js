// Import Dependencies
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

// Create component
const RestaurantList = () => {
  return (
    <View>
      <Text>Restaurant List Component</Text>
    </View>
  );
};

// Create Stylesheet
const styles = StyleSheet.create({});

// Export Component
export default RestaurantList;